# kubeship/helm-charts/auth-service/values.yaml

replicaCount: 1

image:
  repository: ""
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8001
  targetPort: 8001

fullnameOverride: auth-service